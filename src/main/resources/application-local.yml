spring:
  application:
    name: simulator
  datasource:
    url: jdbc:h2:mem:sim
    username: sa
    password:
  docker:
    compose:
      enabled: false
  flyway:
    locations: classpath:db/migration
  h2:
    console:
      enabled: true
